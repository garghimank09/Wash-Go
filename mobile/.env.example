# Copy to .env and set your machine's LAN IP for physical device testing.
# iOS Simulator can use http://127.0.0.1:8000 (default).
# Android Emulator uses http://10.0.2.2:8000 by default when unset.
EXPO_PUBLIC_API_URL=http://192.168.1.42:8000
