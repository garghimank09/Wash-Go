import WaterRingRevealSplashLite from './WaterRingRevealSplashLite';

/** Expo Go–safe splash (no Blur, Moti, or animated SVG). */
export default function WaterRingRevealSplash(props) {
  return <WaterRingRevealSplashLite {...props} />;
}
